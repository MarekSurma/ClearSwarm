# OpenAI-compatible API Configuration
OPENAI_API_BASE=https://openrouter.ai/api/v1
OPENAI_API_KEY=your-openrouter-api-key-here
OPENAI_MODEL=qwen/qwen3-vl-8b-instruct

# Master key used to encrypt model-connection API keys stored in the database.
# Optional: if unset, a key is auto-generated and saved to .clearswarm_secret.key.
# Generate one with: python -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
# CLEARSWARM_SECRET_KEY=
